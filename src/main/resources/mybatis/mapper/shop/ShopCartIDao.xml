<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.ama.don.shop.dao.ShopIDao">


	<insert id="cart_write">
		insert into cart(user_id,product_id,cart_quantity,cart_date) 
		values(#{param1}, #{param2}, #{param3},now());
	</insert> 
	
	<select id="cart_list" resultType="com.ama.don.shop.dto.CartFlatDto">
		select c.cart_id,c.user_id,c.product_id,c.cart_quantity,c.cart_date,u.user_name,p.product_name,p.product_price,p.product_mall_name,p.product_istoday,p.product_discountrate,pi.product_imgurl
		from cart c 
		join user_detail u on c.user_id=u.user_id
		join product p on c.product_id=p.product_id
		join product_img pi on p.product_id=pi.product_id 
		and pi.product_img_type = '메인'
		where u.user_id= #{user_id};
		order by c.cart_date desc
	</select>
	
	
	<select id="cart_list_flat" parameterType="Long" resultType="com.ama.don.shop.dto.CartFlatDto">
        select 
            c.cart_id as cart_id,
            c.cart_quantity as cart_quantity,
            u.user_id as user_id,
            u.user_name as user_name,
            p.product_id as product_id,
            p.product_name as product_name,
            p.product_mall_name as product_mall_name,
            p.product_price as product_price,
            p.product_discountrate as product_discountrate,
            pi.product_imgurl as product_imgurl,
            pi.product_img_type as product_img_type
        from cart c
        join user_detail u on c.user_id = u.user_id
        join product p on c.product_id = p.product_id
        left join product_img pi on p.product_id = pi.product_id
            and pi.product_img_type = '메인'
        where u.user_id = #{user_id}
        order by c.cart_date desc
    </select>
    
    
   	<!-- 장바구니 비우기 -->
    <delete id="cart_clear" parameterType="Long">
        delete from cart 
        where user_id = #{user_id}
    </delete>
    
      <!-- 5-1. 장바구니 단일 품목 지우기 -->
    <delete id="cart_delete_item" parameterType="map">
        delete from cart 
        where user_id = #{user_id} and product_id = #{product_id}
    </delete>
    
      <!-- 장바구니 단일 품목 수정 -->
    <update id="cart_update" parameterType="map">
        update cart 
    	set cart_quantity = #{cart_quantity}
    	where cart_id = #{cart_id}
    </update>
    
    
    
    
</mapper>	