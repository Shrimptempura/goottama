<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ama.don.community.dao.CommunityCommentDao">

	<!-- 댓글 등록 -->
	<insert id="insert"
		parameterType="com.ama.don.community.dto.Comment.CommentCreateDto"
		useGeneratedKeys="true" keyProperty="comment_id">
		INSERT INTO comments (
		user_id,
		comment_content,
		parent_comment_id,
		target_id,
		target_type
		)
		VALUES (
		#{user_id},
		#{comment_content},
		#{parent_comment_id},
		#{targetId},
		#{targetType}
		)
	</insert>

	<!-- 특정 타겟에 달린 댓글 목록 -->
	<select id="findByTargetId"
		resultType="com.ama.don.community.dto.Comment.CommentCreateDto">
		SELECT *
		FROM comments
		WHERE target_id = #{targetId}
		AND target_type = #{targetType}
		AND is_deleted = 0
		ORDER BY created_at ASC
	</select>

	<!-- 댓글 단건 조회 -->
	<select id="findById"
		resultType="com.ama.don.community.dto.Comment.CommentCreateDto">
		SELECT *
		FROM comments
		WHERE comment_id = #{commentId}
		AND is_deleted = 0
	</select>

	<!-- 댓글 삭제  -->
	<update id="delete">
		UPDATE comments
		SET is_deleted = 1,
		modified_at = CURRENT_TIMESTAMP
		WHERE comment_id = #{commentId}
	</update>

	<!-- 댓글 수정 -->
	<update id="update"
		parameterType="com.ama.don.community.dto.Comment.CommentCreateDto">
		UPDATE comments
		SET comment_content = #{comment_content},
		modified_at = CURRENT_TIMESTAMP
		WHERE comment_id = #{comment_id}
	</update>

</mapper>
