<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ama.don.admin.temp.FileIDao">

    <insert id="insertFile" parameterType="com.ama.don.common.dto.FileDto" useGeneratedKeys="true" keyProperty="file_id">
        INSERT INTO file (file_uploader, file_name, file_path, target_type, target_id)
        VALUES (#{file_uploader}, #{file_name}, #{file_path}, #{target_type}, #{target_id})
    </insert>

    <select id="getFilesByTarget" resultType="com.ama.don.common.dto.FileDto">
        SELECT file_id, file_uploader, file_name, file_path, target_type, target_id
        FROM file
        WHERE target_type = #{targetType}
        AND target_id = #{targetId}
        ORDER BY file_id ASC
    </select>

    <select id="getFileById" resultType="com.ama.don.common.dto.FileDto">
        SELECT file_id, file_uploader, file_name, file_path, target_type, target_id
        FROM file
        WHERE file_id = #{fileId}
    </select>

    <delete id="deleteFile">
        DELETE FROM file
        WHERE file_id = #{fileId}
    </delete>

    <delete id="deleteFilesByTarget">
        DELETE FROM file
        WHERE target_type = #{targetType}
        AND target_id = #{targetId}
    </delete>

    <update id="updateFilesTargetAndUploader">
        UPDATE file
        SET target_id = #{targetId}
        WHERE file_uploader = 'TUI_EDITOR'
        AND target_type = 'ADMIN'
        AND target_id &lt; 0
        AND file_path IN
        <foreach collection="filePaths" item="filePath" open="(" separator="," close=")">
            #{filePath}
        </foreach>
    </update>

    <select id="getFilesByTargetAndUploader" resultType="com.ama.don.common.dto.FileDto">
        SELECT file_id, file_name, file_path, file_uploader, target_type, target_id
        FROM file
        WHERE target_type = #{targetType}
        AND file_uploader = #{fileUploader}
        AND target_id = #{targetId}
    </select>

    <select id="getNegativeTargetIdFiles" resultType="com.ama.don.common.dto.FileDto">
        SELECT file_id, file_name, file_path, file_uploader, target_type, target_id
        FROM file
        WHERE target_id &lt; 0
        AND file_uploader = 'TUI_EDITOR'
    </select>

</mapper>