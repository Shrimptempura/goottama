<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ama.don.admin.dao.ManageUserIDao">
    <select id="getAllUsers">
        SELECT D.USER_ID, D.USER_NAME, D.USER_NICKNAME, D.USER_CREATED_AT, L.ROLES_ID, L.LOGIN_ID, D.USER_STATUS, D.USER_SANCTIONS_UNTIL
        FROM user_detail D
        JOIN user_login L ON D.USER_ID = L.USER_ID
        LIMIT #{searchVO.rowStart}, #{searchVO.displayRowCount}
    </select>

    <select id="countAllUsers">
        SELECT COUNT(*) FROM user_detail
    </select>

    <select id="getUserMin">
        SELECT D.USER_ID, D.USER_NAME, D.USER_NICKNAME, D.USER_CREATED_AT, L.ROLES_ID]
        FROM user_detail D
        JOIN user_login L ON D.USER_ID = L.USER_ID
    </select>

    <select id="getUserByUserId" resultType="com.ama.don.admin.dto.userDTO.UserTotalDataDTO">
        SELECT D.USER_ID, D.USER_NAME, D.USER_NICKNAME, D.USER_GENDER, D.USER_BIRTH, D.USER_CREATED_AT, D.USER_TEL, D.USER_ZIPCODE, D.USER_ADDR, D.USER_EMAIL, D.USER_IMG,
        D.USER_STATUS, D.USER_SANCTIONS_UNTIL,
        L.ROLES_ID, L.LOGIN_ID, L.USER_PASSWORD
        FROM user_detail D
        JOIN user_login L ON D.USER_ID = L.USER_ID
        WHERE D.USER_ID = #{param1}
    </select>

    <select id="getUserByLoginId" resultType="com.ama.don.admin.dto.userDTO.UserTotalDataDTO">
        SELECT D.USER_ID, D.USER_NAME, D.USER_NICKNAME, D.USER_GENDER, D.USER_BIRTH, D.USER_CREATED_AT, D.USER_TEL, D.USER_ZIPCODE, D.USER_ADDR, D.USER_EMAIL, D.USER_IMG,
        D.USER_STATUS, D.USER_SANCTIONS_UNTIL,
        L.ROLES_ID, L.LOGIN_ID, L.USER_PASSWORD
        FROM user_detail D
        JOIN user_login L ON D.USER_ID = L.USER_ID
        WHERE L.LOGIN_ID = #{param1}
    </select>

    <update id="updateUserSanctionsUntil">
        UPDATE user_detail
        SET USER_SANCTIONS_UNTIL = #{endDate}
        WHERE USER_ID = #{userId}
    </update>

    <update id="updateUserSanctionsAndStatus">
        UPDATE user_detail
        SET
        USER_SANCTIONS_UNTIL = #{endDate},
        USER_STATUS = #{userStatus}
        WHERE USER_ID = #{userId}
    </update>

    <update id="resetExpiredUserSanctions">
        UPDATE user_detail
        SET
        USER_SANCTIONS_UNTIL = NULL,
        USER_STATUS = 'active'
        WHERE user_sanctions_until IS NOT NULL AND user_sanctions_until &lt; NOW()
    </update>

    <update id="changeUserRole">
        UPDATE user_login
        SET
        ROLES_ID = #{role}
        WHERE USER_ID = #{userId}
    </update>

    <select id="searchUsers" resultType="com.ama.don.admin.dto.userDTO.UserTotalDataDTO">
        SELECT
        D.USER_ID, D.USER_NAME, D.USER_NICKNAME, D.USER_GENDER, D.USER_BIRTH, D.USER_CREATED_AT, D.USER_TEL, D.USER_ZIPCODE, D.USER_ADDR, D.USER_EMAIL, D.USER_IMG, D.USER_STATUS, D.USER_SANCTIONS_UNTIL, L.ROLES_ID, L.LOGIN_ID
        FROM
        user_detail D
        JOIN
        user_login L ON D.USER_ID = L.USER_ID
        <where>
            <if test="userSearchDTO.user_name != null and userSearchDTO.user_name != ''">
                AND D.USER_NAME LIKE CONCAT('%', #{userSearchDTO.user_name}, '%')
            </if>
            <if test="userSearchDTO.user_status != null and userSearchDTO.user_status != ''">
                AND D.USER_STATUS IN
                <foreach collection="userSearchDTO.user_status" item="status" open="(" separator="," close=")">
                    #{status}
                </foreach>
            </if>
            <if test="userSearchDTO.user_nickname != null and userSearchDTO.user_nickname != ''">
                AND D.USER_NICKNAME LIKE CONCAT('%', #{userSearchDTO.user_nickname}, '%')
            </if>
            <if test="userSearchDTO.user_gender != null and userSearchDTO.user_gender != ''">
                AND D.USER_GENDER = #{userSearchDTO.user_gender}
            </if>
            <if test="userSearchDTO.create_start_date != null and userSearchDTO.create_start_date != ''">
                AND D.USER_CREATED_AT &gt;= #{userSearchDTO.create_start_date}
            </if>
            <if test="userSearchDTO.create_end_date != null and userSearchDTO.create_end_date != ''">
                AND D.USER_CREATED_AT &lt;= #{userSearchDTO.create_end_date}
            </if>
            <if test="userSearchDTO.user_tel != null and userSearchDTO.user_tel != ''">
                AND D.USER_TEL LIKE CONCAT('%', #{userSearchDTO.user_tel}, '%')
            </if>
            <if test="userSearchDTO.user_zipcode != null and userSearchDTO.user_zipcode != ''">
                AND D.USER_ZIPCODE LIKE CONCAT('%', #{userSearchDTO.user_zipcode}, '%')
            </if>
            <if test="userSearchDTO.user_addr != null and userSearchDTO.user_addr != ''">
                AND D.USER_ADDR LIKE CONCAT('%', #{userSearchDTO.user_addr}, '%')
            </if>
            <if test="userSearchDTO.user_email != null and userSearchDTO.user_email != ''">
                AND D.USER_EMAIL LIKE CONCAT('%', #{userSearchDTO.user_email}, '%')
            </if>
        </where>
        LIMIT #{searchVO.rowStart}, #{searchVO.displayRowCount}
    </select>
    <select id="countSearchUsers" resultType="int">
        SELECT COUNT(*) FROM user_detail
        <where>
            <if test="userSearchDTO.user_name != null and userSearchDTO.user_name != ''">
                AND USER_NAME LIKE CONCAT('%', #{userSearchDTO.user_name}, '%')
            </if>
            <if test="userSearchDTO.user_status != null and userSearchDTO.user_status != ''">
                AND USER_STATUS IN
                <foreach collection="userSearchDTO.user_status" item="status" open="(" separator="," close=")">
                    #{status}
                </foreach>
            </if>
            <if test="userSearchDTO.user_nickname != null and userSearchDTO.user_nickname != ''">
                AND USER_NICKNAME LIKE CONCAT('%', #{userSearchDTO.user_nickname}, '%')
            </if>
            <if test="userSearchDTO.user_gender != null and userSearchDTO.user_gender != ''">
                AND USER_GENDER = #{userSearchDTO.user_gender}
            </if>
            <if test="userSearchDTO.create_start_date != null and userSearchDTO.create_start_date != ''">
                AND USER_CREATED_AT &gt;= #{userSearchDTO.create_start_date}
            </if>
            <if test="userSearchDTO.create_end_date != null and userSearchDTO.create_end_date != ''">
                AND USER_CREATED_AT &lt;= #{userSearchDTO.create_end_date}
            </if>
            <if test="userSearchDTO.user_tel != null and userSearchDTO.user_tel != ''">
                AND USER_TEL LIKE CONCAT('%', #{userSearchDTO.user_tel}, '%')
            </if>
            <if test="userSearchDTO.user_zipcode != null and userSearchDTO.user_zipcode != ''">
                AND USER_ZIPCODE LIKE CONCAT('%', #{userSearchDTO.user_zipcode}, '%')
            </if>
            <if test="userSearchDTO.user_addr != null and userSearchDTO.user_addr != ''">
                AND USER_ADDR LIKE CONCAT('%', #{userSearchDTO.user_addr}, '%')
            </if>
            <if test="userSearchDTO.user_email != null and userSearchDTO.user_email != ''">
                AND USER_EMAIL LIKE CONCAT('%', #{userSearchDTO.user_email}, '%')
            </if>
        </where>
    </select>
</mapper>