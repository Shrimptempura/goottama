<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ama.don.admin.dao.NoticesIDao">
    <!--  공지 모든 공지 추출  -->
    <select id="getAllNotices">
        SELECT NOTICES_ID, NOTICES_TITLE, NOTICES_IS_PINNED, NOTICES_CREATED_AT, NOTICES_FILE_PATH, NOTICES_CONTENT
        FROM notices ORDER BY NOTICES_ID
    </select>

    <!--  아이디로 공지 검색  -->
    <select id="getNoticeById">
        SELECT NOTICES_ID, NOTICES_TITLE, NOTICES_IS_PINNED, NOTICES_CREATED_AT, NOTICES_FILE_PATH, NOTICES_CONTENT
        FROM notices WHERE NOTICES_ID=#{param1}
    </select>


    <!--  공지를 제목, 내용, 작성일로 검색  -->
    <!--  param1:검색타입, param2:검색내용  -->
    <sql id="searchCondition">
        <choose>
            <when test="param1==1">
                WHERE NOTICES_TITLE LIKE CONCAT('%', #{param2}, '%')
            </when>
            <when test="param1==2">
                WHERE NOTICES_CONTENT LIKE CONCAT('%', #{param2}, '%')
            </when>
            <when test="param1==3">
                WHERE (NOTICES_TITLE LIKE CONCAT('%', #{param2}, '%')
                OR NOTICES_CONTENT LIKE CONCAT('%', #{param2}, '%'))
            </when>
            <when test="param1==4">
                WHERE NOTICES_CREATED_AT==#{param2}
            </when>
            <otherwise></otherwise>
        </choose>
    </sql>

    <select id="searchNotice">
        SELECT NOTICES_ID, NOTICES_TITLE, NOTICES_IS_PINNED, NOTICES_CREATED_AT, NOTICES_FILE_PATH, NOTICES_CONTENT
        FROM notices
        <include refid="searchCondition" />
        ORDER BY NOTICES_ID
    </select>

    <!--  날짜 기간으로 공지 검색 param1:시작날짜, param2:종료 날짜 -->
    <select id="searchNoticesByDateRange">
        SELECT NOTICES_ID, NOTICES_TITLE, NOTICES_IS_PINNED, NOTICES_CREATED_AT, NOTICES_FILE_PATH, NOTICES_CONTENT
        FROM notices
        BETWEEN #{param1} AND #{param2}
    </select>

    <!-- 공지 수정 -->
    <update id="modifyNotice">
        UPDATE notices
        SET
        NOTICES_TITLE = #{param2},
        NOTICES_IS_PINNED =#{param3},
        NOTICES_FILE_PATH =#{param4},
        NOTICES_CONTENT =#{param5}
        WHERE NOTICES_ID = #{param1}
    </update>

    <!--  공지 작성  -->
    <insert id="writeNotice">
        INSERT INTO notices(NOTICES_CREATED_AT, NOTICES_TITLE, NOTICES_IS_PINNED, NOTICES_FILE_PATH, NOTICES_CONTENT)
        VALUES(NOW(), #{param1}, #{param2}, #{param3}, #{param4})
    </insert>

    <!--  공지 삭제  -->
    <delete id="deleteNotice">
        DELETE FROM notices WHERE NOTICES_ID = #{param1}
    </delete>
</mapper>