<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://.org/dtdmybatis/mybatis-3-mapper.dtd">

<mapper namespace="com.ama.don.member.dao.InquiryDao">
	
	<insert id="insertInquiryKakao">
	insert into inquiry(user_id,inquiry_way_id) values(#{user_id},#{inquity_id})
	</insert>
	
	<insert id="insertInquiryEmail">
	insert into inquiry(user_id,inquiry_way_id) values(#{user_id},#{inquity_id})
	</insert>

</mapper>